# Connect IoT - Your Tasks/Converters Library Package

This a package that only works for ESM, which will be released in version 12 of CM MES.

The node-llama-cpp does not work with ZoneJs and only with context NodeJs which is released in version 11.3 of CM MES.

There are also issues with packaging with ncc that at the time of this implementation have not yet been fixed.
